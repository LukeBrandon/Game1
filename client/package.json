{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start:dev": "npm-run-all --parallel local build:dev",
    "start": "google-chrome index.html",
    "build:dev": "webpack",
    "lint": "tslint --format prose src/**/*.ts --force",
    "watch:lint": "nodemon -x 'npm run lint' --silent --watch src/ -e ts",
    "local": "ws --port 8005"
  },
  "nodemonConfig": {
    "verbose": false,
    "ignore": "bin/*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/socket.io-client": "^1.4.32",
    "nodemon": "^1.18.7",
    "npm-run-all": "^4.1.3",
    "ts-loader": "^5.3.0",
    "tslint": "^5.11.0",
    "typescript": "^3.1.6",
    "webpack": "^4.23.1",
    "webpack-cli": "^3.1.2"
  },
  "dependencies": {
    "local-web-server": "^2.6.0",
    "socket.io-client": "2.2.0"
  }
}
